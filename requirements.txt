# ============================================================
# PaperMill Hunter - 依赖清单
# 分层组织：采集 / 存储 / 分析 / 可视化 / 工程
# ============================================================

# ---------- 采集层 (Ingestion) ----------
# httpx: 支持 HTTP/2、连接池、超时的现代 HTTP 客户端，比 requests 更适合高并发采集
httpx>=0.28.1
# tenacity: 通用重试库，用来实现"指数退避 + 抖动"的失败重试
tenacity>=9.1.4

# ---------- 数据处理与存储 (Storage) ----------
pandas>=2.2.3
numpy>=2.1.3
pyarrow>=16.1.0
# duckdb: 嵌入式列式分析数据库，零部署、单文件，直接对 Parquet 跑 SQL
duckdb>=1.1.0

# ---------- 分析层 (Analysis) ----------
scikit-learn>=1.5.0
scipy>=1.14.0
# networkx: 图/网络分析，用于合作网络与引用闭环检测
networkx>=3.4

# ---------- 可视化 (Visualization) ----------
plotly>=5.24.0
streamlit>=1.40.0
matplotlib>=3.9.0

# ---------- 配置与工程 (Engineering) ----------
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
structlog>=24.4.0

# ---------- 开发与测试 (Dev / Test) ----------
pytest>=8.3.0
pytest-cov>=6.0.0
ruff>=0.8.0
